interface:
  display_name: "CircuitLab"
  short_description: "Acquire MCU, sensor, and support IC assets"
  brand_color: "#6DFF9D"
  default_prompt: "Use $circuitlab to acquire and inspect exact packaged MCU, SoC, sensor, and support-IC assets in a local circuit-development workspace."
